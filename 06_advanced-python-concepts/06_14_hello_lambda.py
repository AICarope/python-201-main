# Create a lambda expression that takes no input
# and prints "hello world" to the console. What does it return?

no_arg_lambda = lambda: "Hello, World!"
print(no_arg_lambda())  # Output will be "Hello, World!"